package Spring.eg8;
import org.springframework.context.annotation.*;

@Configuration
@ComponentScan(basePackages = "Spring.eg8")
public class config 
{
	/*
	 * @Bean
	 * 
	 * @Scope("singleton") public Address getAddress() { Address add = new Address
	 * (); add.setDoorno(342); add.setArea("Ellis Nagar"); add.setCity("Madurai");
	 * return add; }
	 * 
	 * @Bean (name = "stu") public Student student() { Student stu = new Student ();
	 * stu.setId(202001); stu.setName("Mrunaal Thaakur"); return stu; }
	 */
}
