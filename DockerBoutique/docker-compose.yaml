version: "3.7"
 
# Define services
services:
  # App backend service
  backend:
    # Configuration for building the docker image for the backend service
    image: boutique-back-end
    container_name: boutique-back-end
    build:
      context: ./backend # Use an image built from the specified dockerfile in the `polling-app-server` directory.
    ports:
      - "4444:4444" # Forward the exposed port 8080 on the container to port 8080 on the host machine
    # restart: always
    depends_on:
      - db # This service depends on mysql. Start that first.
    environment: # Pass environment variables to the service
      SPRING_DATASOURCE_URL: jdbc:mysql://172.29.0.2:3306/boutique_db?createDatabaseIfNotExist=true
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: Srienath123@V
    networks: # Networks to join (Services on the same network can communicate with each other using their name)
      - backend
      - frontend
 
  # Frontend Service
 
  frontend:
    image: boutique-front-end
    container_name: boutique-front-end
    build:
      context: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
    networks:
      - frontend
  # Database Service (Mysql)
  db:
    image: mysql:5.7
    ports:
      - "6704:3306"
    restart: always
    environment:
      MYSQL_DATABASE: boutique_db
      # MYSQL_USER: root
      # MYSQL_PASSWORD: Admin@123
      MYSQL_ROOT_PASSWORD: Srienath123@V
    volumes:
      - db-data:/var/lib/mysql
    networks:
      - backend
# Volumes
volumes:
  db-data:
 
# Networks to be created to facilitate communication between containers
networks:
  backend:
  frontend: