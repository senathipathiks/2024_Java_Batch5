import React, { Component } from 'react'

export class Greet extends Component {
  render() {
    return (
      <div>
        <h1>Greet, Named export we can't change the name </h1>
      </div>
    )
  }
}

// export default Greet;