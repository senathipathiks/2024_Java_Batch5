let arr = [1,2,3,4,5]
let mon = Math.min(...arr)
console.log(mon);

// spread operator -- spread the values in the variable and perform the function separately. it is invoked in the function calling.